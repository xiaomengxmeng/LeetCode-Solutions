# 📝 2026-04-29 学习记录

## [Medium] Next Greater Element II (503)

### 🎯 核心思路
循环数组的单调栈问题。遍历 2n 次，用 `i % n` 取真实下标模拟循环。关键优化：只在第一圈入栈（`i < n`），第二圈只处理栈里剩余元素。结果数组初始化为 -1。

### 🔑 关键代码片段
```cpp
vector<int> nextGreaterElements(vector<int>& nums) {
    int n = nums.size();
    vector<int> res(n, -1);
    stack<int> st;
    for (int i = 0; i < n * 2; i++) {
        while (!st.empty() && nums[st.top()] < nums[i % n]) {
            res[st.top()] = nums[i % n];
            st.pop();
        }
        if (i < n) {
            st.push(i);
        }
    }
    return res;
}
```

### 📚 学到的知识点
- **循环数组处理**：遍历 2n 次，用 `i % n` 取模
- **只入栈一次**：`if (i < n)` 避免重复入栈
- **结果初始化为 -1**：没找到答案的自动返回 -1

### ⚠️ 易错点 & 反思
- ❌ 第二圈也入栈，会重复处理
- ✅ 只在第一圈入栈，第二圈只处理剩余元素
- 💡 **关键优化**：每个元素只需要入栈一次

### 🔗 关联题目
- [496] Next Greater Element I - 单调栈入门
- [739] Daily Temperatures - 单调栈存下标
- [84] Largest Rectangle in Histogram - 单调栈经典题
