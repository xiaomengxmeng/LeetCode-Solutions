# 📝 2026-05-14 思考记录

## \[Medium] Path Sum III (437)

### 🤔 我的原始思路

\[在此记录你的初步想法]

从任意节点到任意后代节点 时间复杂度n^2 

前缀和优化

### 🔍 我的思考过程

\[在此记录逐步思考过程]

数组的前缀和 prefix \[i] =prefix\[i-1]+ array\[i]  i>0 prefix\[0]= array\[0]

树上的前缀和 怎么做 当前节点的前缀和 prefix curr   prefix\[prev]+ cur->val

hash 表  所有路径共享

<br />

回溯的目的是正确追踪当前的前缀和 

map\[0] =1 是？ 

curr\_sum -targetSum =0;

<br />

因为前缀和 无法回溯出路径 那如何列路径呢

### 💡 对比收获

- **暴力DFS（起点枚举）** vs **前缀和+哈希表**：暴力法是正向思维（每个节点为起点向下搜），前缀和是逆向思维（查表找之前的互补和）
- **int 溢出陷阱**：节点值范围 `[-10⁸, 10⁸]`，累加可能超 `INT_MAX`，要用 `long long`
- **map 的 key 类型也必须用 long long**：否则 `curr_sum` 从一个 `long long` 存入 `unordered_map<int,int>` 时会截断，可能误撞到 `map[0]`
- **计数 vs 列路径**：前缀和查表只能计数，不能列路径（哈希表只存了次数，丢了位置信息）
