# 📝 2026-05-29 思考记录

## [Medium] Word Search (79)
### 🤔 我的原始思路
[在此记录你的初步想法]
从所有单词开头字母位置开始DFS
如何标记 如何恢复
找到单词中所有字母  字母的四个方向字母都不符合剪枝
### 🔍 我的思考过程
- 在每个匹配 word[0] 的位置启动 DFS
- 边界检查 + 字母匹配 → 标记 → 递归四方向 → 恢复
- 终止条件：k == word.size()-1 说明所有字母都匹配了

### 💡 对比收获
- **网格 DFS (200/695) vs 网格回溯 (79)**：前者标记后不需要恢复（每个格子只访问一次），后者必须恢复（一条路径失败后要重新尝试）
- **39 vs 79 的剪枝**：39 排序后 break 剪枝；79 用边界检查 + 字母不匹配提前 return

## [Medium] Generate Parentheses (22)
### 🤔 我的原始思路
[在此记录你的初步想法]
括号一一匹配，不能错误闭合 
决策点：每个位置是放 '(' 还是 ')'
但哪些情况不能放 ) ？什么情况下不能再放 ( ？
放 ( 的条件：left < n
放 ) 的条件：right < left
终止条件：括号数量等于 n
### 🔍 我的思考过程
- 用 left/right 剩余数量控制分支
- left>0 放 (；right>left 放 )
- 剩余数量为 0 时记录结果

### 💡 对比收获
- **22 vs 其他回溯**：不需要 used / start，而是用两个计数器约束——模式不同但框架一致

## [Medium] Letter Combinations of a Phone Number (17)
### 🤔 我的原始思路
[在此记录你的初步想法]
映射+回溯
第0步有对应映射种选择
映射表：数字到字母的映射关系用哈希表存储
终止条件：k == digits.size() 说明所有数字都处理了
### 🔍 我的思考过程
[在此记录逐步思考过程]
### 💡 对比收获
- **17 vs 22**：22 每一步只有 2 种选择（( 或 )），且受计数约束；17 每一步有多个选择（映射中的字母），无约束
- **string 数组比哈希表更简洁**：`mapping[digits[k]-'2']` 一行搞定
