# 📝 2026-05-18 思考记录

## \[Medium] Number of Islands (200)

### 🤔 我的原始思路

\[在此记录你的初步想法]

DFS

从 0,0 开始向右 和下方搜索

问题 1：搜索方向

四个方向搜索

问题 2：怎么找到下一个岛屿的起点？

找到岛屿水淹将其改为0

### 🔍 我的思考过程

\[在此记录逐步思考过程]

### 💡 对比收获

- **网格 DFS vs 树 DFS**：树是 left/right，网格是四方向，本质都是递归 + 标记已访问
- **水淹法省空间**：直接在原数组改 `'1'→'0'`，不需要额外 visited 数组
- **外层扫描 + 内层 DFS**：双层 for 找起点，DFS 淹岛屿，分工明确

## \[Medium] Clone Graph (133)

### 🤔 我的原始思路

\[在此记录你的初步想法]

深拷贝 Clone 出的图的节点和原节点是两个节点

bool isVisited 数组标记已经访问过的节点

BFS 

### 🔍 我的思考过程

\[在此记录逐步思考过程]

这就是你说的 bool isVisited 要做的事——但这里还需要 把原节点和新节点一一对应 存起来。

用什么数据结构来存这个映射关系？

使用map 来存

### 💡 对比收获

- **图 DFS vs 树 DFS**：树天然无环，图有环 → 需要 visited 映射防重复
- **DFS 深拷贝模板**：查 map → 创建 → 先存再递归 → 返回
- **网格图 vs 节点图**：200 用水淹法标记，133 用哈希表映射；共同点都是 DFS + 标记

## \[Medium] Course Schedule (207)

### 🤔 我的原始思路

\[在此记录你的初步想法]

有向图怎么表示？ 图表示有数组和链表法

这题感觉可以用数组 \[a,b] =1  表示 b-> a  \[b,a] =1 a-> b

怎么判断有环？

沿有向边 最终回到访问过的节点

<br />

入度表 表来计入每个节点的入度和出度

<br />

### 🔍 我的思考过程

\[在此记录逐步思考过程]

### 💡 对比收获

- **有向图环检测**：三色 DFS（0未访/1访问中/2已完成），遇到 1 就是环
- **无向图 vs 有向图**：无向图用 visited+visiting 两个 bool 即可；有向图需要三态
- **邻接表 vs 邻接矩阵**：稀疏图（课程最多 2000 门）用邻接表省空间
